// TODO: + Enum async или sync
// TODO: + Блоки кода
// TODO: +- sync vs async

// TODO: + hasError ( при при обращении к сесси если true удаляем из SM)
// TODO: + Убрвть future
// TODO: + тесты на последовательную запись и чтение

// TODO: Почитать про динамическую и статическую линковку
// TODO: Заменить shared_ptr на unique_ptr в read, write TransportHandler

// + todo: Передеалать в handlers socket& -> std::shared_ptr<socket>
// + todo: рефакторить TransportHandler

// + todo: Отказать от std::enable_shared_from_this

// + todo
	auto errorHandler = [this](error_code error, TransportMessage& transportMessage, const std::string& func) {
		if(ErrorHandler::check_error(error, std::string("TransportHandler::read.sync") + "{" + func + "}")) {
			if(onError) {
				onError(error);
			}
		}
		if(error) {
			setErrorMessage(transportMessage);
					if(onAllRead) {
            			auto msgCopy = transportMessage;
            			onAllRead(error, std::move(msgCopy));
            		}
			return true;
		}
		return false;
	};


	// TODO: + кОНФИГ КЛИЕНТ ПОРТ НЕ МАССИВ
	// TODO: + структура проекта
	// TODO: + логика magic:
	// в конфиге сервера прописывается для каждого порта свой magic
	// в конфиге клиента пишем magic, который он использует как ключ, добавляя его в данные (обрабатываем только те данные, которые после magic)
	// TODO: + реализации тестовых сообщений, если будет время (протокол общения)

	// + TODO: client_onnection.json и server_connection
	// + TODO: при первом запуске дефолтный порт, magic, ipadd - localhost (чтобы можно было сходу всё запустить на одной машине)
	// + TODO: много портов - много серверов (за запуск открываем ВСЕ порты с их настройками


	// TODO: В конце, если будет нечего делать - делать хорошую документацию в формате md
	// + TODO: дополнить папкой Release gitignore
	// TODO: привести в порядок git

	// + TODO: лютая обертка
